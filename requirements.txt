# Python 3.10. Versions are those of the environment that produced every number
# in the paper (2026-08); pinned as lower bounds.
# OGBench itself is NOT listed: install the pinned checkout (v1.2.1, commit
# 1d41409) as an editable package with its [train] extras and point
# OGBENCH_IMPLS at its impls/ directory (see README.md).
jax>=0.6.2
jaxlib>=0.6.2
flax>=0.10.7
optax>=0.2.8
numpy>=2.2.6
scipy>=1.15.3
mujoco>=3.1.6
gymnasium>=1.3.0
ml_collections>=1.1.0
absl-py>=2.5.0
matplotlib>=3.10.9
tqdm>=4.70.0
imageio>=2.37.4
wandb>=0.28.1
pytest
